/**
 * Re-export barrel for local development.
 *
 * In a consumer project this file is created by `shadcn add`, but inside the
 * registry repo itself we need it so that example files can import from
 * `@/components/ui/generic-input` and pass type-checking.
 */
export { GenericInput } from "@/registry/new-york/ui/generic-input/generic-input"
